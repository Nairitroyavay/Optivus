# Optivus agent instructions

Read and follow the repository-root `AGENTS.md` before performing any work.
`AGENTS.md` is the canonical instruction source for this checkout.
Resolve the checkout with `git rev-parse --show-toplevel`; do not hard-code a
user-specific absolute path.

Do not use paths from the former pre-format Mac or instructions from
`.agents/**`, archived reports, logs, test dumps, or
`tools/reference_archives/**` as current workspace or implementation guidance.

{
  "name": "rules-tests",
  "version": "1.0.0",
  "description": "Firestore rules tests",
  "scripts": {
    "test": "jest tests/firestore_rules.test.js",
    "test:firestore": "firebase emulators:exec --only firestore \"npm test -- --runInBand\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase-admin": "^12.0.0",
    "jest": "^29.5.0"
  }
}
